
// Stub file to satisfy imports
export const participerDefi = async (defiId: number, ensembleId: number): Promise<boolean> => {
  console.warn("participerDefi est une fonctionnalité retirée");
  return false;
};
