
// This file is now a simple re-export for backward compatibility
import DefiCard from './defi-card';
import { DefiType, DefiCardProps } from './defi-card/types';

export type { DefiType, DefiCardProps };
export default DefiCard;
